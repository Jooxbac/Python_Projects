# Docstring template
"""
    Updates a cell if it's possible

    Args:
        cell_number (int): cell to be updated.
        player_piece (str, optional): piece to be set into the cell. Is "X" by default.

    Returns:
        bool: True if cell is updatable, False if not.

    Raises:
        Error type: Possible.
    """